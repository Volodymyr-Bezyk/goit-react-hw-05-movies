import { typography,space,color,flexbox, grid, background, border } from 'styled-system'
import styled from 'styled-components'

const Box = styled('div')(
  typography,space,color,flexbox, grid, background, border
)